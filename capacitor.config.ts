import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Agentes_Transito',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
